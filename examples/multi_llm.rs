use synapse_agentic::prelude::*;

#[tokio::main]
async fn main() -> Result<()> {
    // In a real example we would initialize LLM providers
    println!("Multi-LLM example placeholder");
    Ok(())
}
